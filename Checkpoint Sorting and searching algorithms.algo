Algorithm: InsertionSort(arr)
Input: An array arr of comparable elements
Output: The array arr sorted in ascending order

 n ← length of arr
   for i ← 1 to n - 1 do
     key ← arr[i]
     j ← i - 1
     while j >= 0 and key < arr[j] do
         arr[j + 1] ← arr[j]
         j ← j - 1
     end while
     arr[j + 1] ← key
 end for
